
const crypto = require('crypto');
const addUserUsecase = require('../../../src/domain/usecase/addUserUsecase');

describe('When ...', () => {
  it('It should test', () => {
    expect(1).toBe(1);
  })
});
